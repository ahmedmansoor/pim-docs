---
title: 'List Atoll Initiatives'
api: 'GET /api/v1/atolls/{code}/initiatives'
---

Returns a list of all initiatives in a specific atoll.

### Authorizations

<ParamField header="Authorization" type="string" required>
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

### Path Parameters

<ParamField path="code" type="string" required>
  The atoll code (e.g., "HA")
</ParamField>

### Query Parameters

<ParamField query="limit" type="integer">
  The maximum number of results to return
</ParamField>

<ParamField query="offset" type="integer">
  Number of results to skip for pagination
</ParamField>

### Response

<ResponseField name="success" type="boolean" required>
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="array" required>
  List of initiatives in the atoll
</ResponseField>

<ResponseExample>
```json
{
  "success": true,
  "data": [
    {
      "uuid": "123",
      "name_english": "Harbor Development Project",
      "name_dhivehi": "ބަނދަރު ތަރައްޤީކުރުމުގެ މަޝްރޫޢު",
      "current_scope": "Development of harbor facilities",
      "status": {
        "name": "Ongoing",
        "slug": "ongoing",
        "color": "#059669"
      },
      "location": {
        "code": "A01",
        "name": "HA. Dhidhdhoo",
        "atoll_code": "HA",
        "atoll_code_dv": "ހއ",
        "atoll_name": "Haa Alif Atoll",
        "atoll_name_dv": "ހއ އަތޮޅު",
        "island_code": "LD0101",
        "island_name": "Dhidhdhoo",
        "island_name_dv": "ދިއްދޫ"
      }
    }
  ],
  "links": {},
  "meta": {}
}
```
</ResponseExample> 