---
title: 'List Atoll Initiatives'
api: 'GET /api/v1/atolls/{code}/initiatives'
---

Returns a list of all initiatives in a specific atoll.

### Path Parameters

<ParamField path="code" type="string" required>
  The atoll code (e.g., "HA")
</ParamField>

### Query Parameters

<ParamField query="limit" type="integer">
  The maximum number of results to return
</ParamField>

<ParamField query="offset" type="integer">
  Number of results to skip for pagination
</ParamField>

### Response

<ResponseField name="success" type="boolean" required>
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="array" required>
  List of initiatives in the atoll
</ResponseField>

<ResponseField name="meta" type="object" required>
  Pagination metadata
</ResponseField>

<ResponseExample>
```json
{
  "success": true,
  "data": [
    {
      "id": "123",
      "title": "Harbor Development",
      "description": "Development of harbor facilities",
      "status": "ongoing",
      "location": {
        "atoll_code": "HA",
        "island_name": "Dhiddhoo",
        "island_name_dv": "ދިއްދޫ"
      }
    }
  ],
  "meta": {
    "total": 50,
    "limit": 10,
    "offset": 0
  }
}
```
</ResponseExample> 