---
title: 'Get Function'
api: 'GET /api/v1/functions/{id}'
---

Returns details of a specific function.

### Authorizations

<ParamField header="Authorization" type="string" required>
  Bearer authentication header of the form `Bearer <token>`, where `<token>` is your auth token.
</ParamField>

### Path Parameters

<ParamField path="id" type="string" required>
  The function ID
</ParamField>

### Response

<ResponseField name="success" type="boolean" required>
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="object" required>
  Function details
</ResponseField>

<ResponseExample>
```json
{
  "success": true,
  "data": {
    "uuid": "123",
    "name": "Water Drainage System",
    "name_dv": "ފެން ހިންދުމުގެ ނިޒާމް",
    "description": "Infrastructure for managing and controlling water runoff",
    "slug": "water-drainage-system",
    "initiatives_count": 45
  }
}
```
</ResponseExample> 